# Verified working combination on a 4 x RTX 4090 (Ubuntu 22.04, NVIDIA driver 550, CUDA 12.4):
#
#   torch        2.6.0+cu124
#   torchvision  0.21.0+cu124
#
# IMPORTANT: torch and torchvision MUST be the same CUDA build as your driver.
# If you are on CUDA 12.4, install via:
#
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.6.0 torchvision==0.21.0
#
# Mainland China users may use the Tsinghua mirror instead:
#
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple --only-binary=:all: torch==2.6.0 torchvision==0.21.0
#
# Mismatched torch/cuDNN/driver versions will silently fall back to CPU and slow
# the run by 100x, or throw CUDNN_STATUS_NOT_INITIALIZED on the first conv layer.

torch==2.6.0
torchvision==0.21.0
transformers>=4.44,<6
peft>=0.13
datasets>=2.21
accelerate>=0.34
scikit-learn>=1.3
numpy>=1.24
pyyaml>=6.0
tqdm>=4.65
sentencepiece>=0.1.99
protobuf
evaluate>=0.4
scipy
hf_transfer            # faster HF downloads (optional but recommended)
